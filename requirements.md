# redmine_non_working_days 要件定義書 v1.2

> 前バージョン（v1.1）からの主な変更：隔週ルール起点の明確化、API パラメータ仕様確定、年ナビ追加、定期ルール開始日の省略可能化、重複登録許容、色優先順位確定、キャッシュ戦略確定

---

## 1. 概要

Redmine に「特定日付・定期ルール・各国祝日」を非稼働日として登録する機能を追加するプラグイン。
登録された非稼働日は、Redmine 標準の曜日指定休業日（`Setting.non_working_week_days`）と同等の扱いとし、
Gantt チャート・カレンダー・チケット日付計算すべてに反映する。

また、REST API で非稼働日データを外部公開する機能を備える。
Redmine と連携する他システムが非稼働日を参照することで、非稼働日を Redmine で一元管理できる。

---

## 2. 非稼働日の種別

| 種別 | 識別子 | 説明 |
|---|---|---|
| 祝日 | `holiday` | 外部 API で取得した各国公祝日 |
| 完全カスタム | `custom_fixed` | 管理者が任意に指定した特定日付 |
| 定期カスタム | `custom_recurring` | 繰り返しルールで自動評価される非稼働日 |

全種別で **休日タイトル**（例："成人の日"、"夏季休暇"）を設定可能。

同一日付に複数種別・複数タイトルの重複登録を許容する。
Redmine 標準休業日（曜日）との重複も許容する。

---

## 3. 機能要件

### 3.1 祝日取得機能（オンライン取得）

- 管理画面から **国（地域）** と **取得年** を指定してワンクリック取得
- データソース：**Nager.Date API**
  - エンドポイント：`GET https://date.nager.at/api/v3/PublicHolidays/{year}/{countryCode}`
  - 認証不要・無料・122 カ国対応
  - API ベース URL は設定画面で変更可能（将来の代替 API 対応）
- 国一覧は `GET /api/v3/AvailableCountries` から取得し、初回取得結果をキャッシュしてドロップダウン表示
- 指定年・指定国の **種別 `holiday` のデータ** が既に登録済みの場合、該当レコードのみ全件破棄して置き換える
  - `custom_fixed` および `custom_recurring` のデータは一切変更しない
  - 実行前に Warning 表示

---

### 3.2 カスタム非稼働日設定機能

#### 3.2.1 完全カスタム（特定日付指定）

- 任意の年月日を指定して登録
- 休日タイトル必須

#### 3.2.2 定期カスタム（繰り返しルール）

**ルール種別：**

| 種別 | 指定内容 | 備考 |
|---|---|---|
| 毎月 n 日 | 1〜31 の日にちを複数指定（最大 31 個） | 該当月に存在しない日はスキップ |
| 毎月末日 | 末日固定（チェックボックスで独立指定） | 月ごとに自動的に 28〜31 日を対象 |
| 毎月第 m × 曜日 | 第 1〜5 × 月〜日 | 該当週が存在しない月はスキップ |
| 隔週 × 曜日 | 開始日・終了日 + 曜日指定 | 下記参照 |

**隔週ルールの起点：**

- 開始日と終了日は DatePicker で必須入力
- 開始日〜終了日の範囲内で **最初に指定曜日が現れる日** を起点とし、以後 2 週ごとに繰り返す
- 例）開始日=2026-04-01（水）、指定曜日=金 → 最初の非稼働日は 2026-04-03（金）、次は 2026-04-17（金）…

> **毎週指定について：**
> 毎週特定曜日を休業日にする場合は、Redmine 標準の
> 「管理 > 設定 > チケットトラッキング > 休業日」で設定すること。

**有効期間：**

- 全ルール共通で開始日・終了日を設定可能（DatePicker）
- **開始日：省略可能**（省略時 = 無制限。過去の日付にも遡及して評価される）
- **終了日：省略可能**（省略時 = 無期限）
- 開始日を設定した場合、その日付以降のみ評価対象となる

> **遡及適用について：**
> 開始日を省略すると過去の Gantt チャートにも反映される。
> 「今日以降のみ適用したい」場合は開始日に本日を指定すること。

---

### 3.3 非稼働日カレンダー表示

プラグイン管理画面内に専用カレンダービューを設置。閲覧・編集ともに管理者のみ。

#### 3.3.1 年カレンダー（上半分）

- 1〜12 月を一覧表示
- 非稼働日は種別ごとに異なる色で塗りつぶし（複数種別が重なる場合は下記優先順位で1色表示）

**表示色と優先順位（高→低）：**

| 優先 | 種別 | 表示色 |
|---|---|---|
| 1 | 祝日（`holiday`） | 赤系 |
| 2 | 完全カスタム（`custom_fixed`） | 青系 |
| 3 | 定期カスタム（`custom_recurring`） | 緑系 |
| 4 | Redmine 標準休業日（曜日） | グレー |

- タイトルは表示しない（省スペース）
- 凡例を表示
- 年ナビゲーション：`[◀ 前年]` `[次年 ▶]`（プルダウンなし）
- 月をクリックすると下の月カレンダーが該当月へジャンプ
- 年カレンダーと月カレンダーは表示年を連動する

#### 3.3.2 月カレンダー（下半分）

- ナビゲーション：`[◀ 前月]` `[当月]` `[次月 ▶]`
- 年カレンダーの月クリックでジャンプ、年をまたぐ前月・次月移動時は年カレンダーも連動して切り替わる
- デフォルト表示：当月
- 各日付に全種別のタイトルを表示（複数登録がある日は複数行）
- **3件を超える日は `+N` 表示し、クリックでポップアップに全件表示**
- Redmine 標準休業日（曜日）もグレーで表示

> **注記：** 通常の曜日指定の休業日は「管理 > 設定 > チケットトラッキング > 休業日」で設定すること。

---

### 3.4 外部公開 API

Redmine と連携する他システムが非稼働日データを参照するための REST API。

#### 3.4.1 エンドポイント

```
GET /non_working_days/api/days.json
```

#### 3.4.2 クエリパラメータ

| パラメータ | 必須 | 説明 | 例 |
|---|---|---|---|
| `year` | 任意 | 取得対象年（省略時：当年） | `2026` |
| `from` | 任意 | 取得開始日（`YYYY-MM-DD`）。`year` より優先 | `2026-04-16` |
| `to` | 任意 | 取得終了日（`YYYY-MM-DD`）。`year` より優先 | `2027-04-15` |
| `kind` | 任意 | 種別絞り込み。カンマ区切りで複数指定可 | `holiday,custom_fixed` |

**`from` / `to` の扱い：**

- `from` と `to` はセットで指定する（片方のみ不可）
- `year` と `from`/`to` が両方指定された場合、`from`/`to` が優先
- 指定範囲に実質的な上限なし（10〜20 年スパンも可）
- サーバー側のみ内部ガード（50 年超は `422`）。ユーザー向けドキュメントへの記載は不要

#### 3.4.3 認証

Redmine 標準の API キー認証に準拠。

- ヘッダー：`X-Redmine-API-Key: {api_key}`
- クエリパラメータ：`?key={api_key}`

Redmine の「管理 > 設定 > API」で REST API が有効になっていることが前提。
REST API が無効の場合、Redmine 本体のフィルタが `403` を返す。プラグイン側の追加実装は不要。

#### 3.4.4 レスポンス形式

```json
{
  "non_working_days": [
    {
      "date": "2026-01-01",
      "title": "元日",
      "kind": "holiday",
      "country_code": "JP"
    },
    {
      "date": "2026-08-13",
      "title": "夏季休暇",
      "kind": "custom_fixed",
      "country_code": null
    },
    {
      "date": "2026-12-28",
      "title": "冬期休暇",
      "kind": "custom_recurring",
      "country_code": null
    }
  ]
}
```

> **注記：** `custom_recurring` 種別はリクエスト時点でルールを展開した結果を返す。
> 呼び出し側はルールの解釈を行う必要はない。

#### 3.4.5 エラーレスポンス

| HTTP ステータス | 原因 |
|---|---|
| `401 Unauthorized` | API キーが無効または未指定 |
| `403 Forbidden` | Redmine の REST API 機能が無効 |
| `422 Unprocessable Entity` | パラメータ形式不正（日付フォーマット誤り・`from`/`to` 片方のみ指定等） |

---

## 4. Redmine 内部への反映

### 4.1 パッチ対象

`Redmine::Utils::DateCalculation` モジュールの以下 3 メソッドを `Module#prepend` でパッチ：

| メソッド | 影響箇所 |
|---|---|
| `working_day?(date)` | 全ての日付判定 |
| `add_working_days(date, days)` | 依存チケットの日付自動計算 |
| `working_days(from, to)` | 稼働日数カウント |

> **実装時確認事項：** パッチ対象のメソッド名は Redmine 本体ソースを確認して確定すること。

### 4.2 評価方式

- `holiday` / `custom_fixed` 種別：キャッシュ上の日付 Set と照合（`Set#include?` で O(1) 判定）
- `custom_recurring` 種別：`working_day?` 呼び出し時にキャッシュ上のルール一覧をメモリ内でオンザフライ評価（有効期間チェック含む）

### 4.3 キャッシュ戦略

**方針：初回アクセス時に全件まとめてキャッシュし、Admin 操作時にクリア。**

| データ | キャッシュ内容 | 展開タイミング |
|---|---|---|
| `holiday` / `custom_fixed` | 日付の Set（高速 include 判定用） | 初回アクセス時・該当データ変更時 |
| `custom_recurring` | ルールオブジェクトの配列 | 初回アクセス時・ルール変更時 |

- キャッシュストア：`Rails.cache` を使用
- バックエンド：**`:file_store`**（`Rails.root/tmp/cache/`）
  - 全 Puma ワーカー（現在 4 プロセス）がファイルシステムを共有するため、invalidate が全プロセスに即時反映
  - `:memory_store` は使用しない（プロセスごとに独立したキャッシュとなり整合性が取れないため）
- 管理画面での保存・削除操作をトリガーに即時 invalidate → 次リクエスト時に再展開
- **保存後は再起動不要で即時反映**
- API レスポンスもキャッシュから生成する（DB アクセスなし）

> **設定例（`config/environments/production.rb`）：**
> ```ruby
> config.cache_store = :file_store, Rails.root.join("tmp/cache")
> ```

### 4.4 Gantt およびカレンダーへの表示反映

- 標準の `nwday` CSS クラスが付与される（標準休業日と同じグレーアウト）
- 種別ごとの色分けは Gantt・Redmine カレンダー上では行わない
- プラグイン管理画面内カレンダーのみ色分けを行う

---

## 5. 権限設計

| 操作 | 必要権限 |
|---|---|
| 非稼働日の登録・編集・削除 | Redmine 管理者のみ |
| 祝日オンライン取得 | Redmine 管理者のみ |
| カレンダー表示 | Redmine 管理者のみ |
| API アクセス（読み取り） | 有効な Redmine API キーを持つユーザー |

---

## 6. スコープ外

- Redmine 標準の曜日指定休業日の変更・管理
- プロジェクト単位・ユーザー単位の個別カレンダー
- 振替休日の自動計算
- 非稼働日データのインポート・エクスポート

---

## 7. 多言語対応（i18n）

GUI 上のすべての表示文字列は英語・日本語に対応する。

- Redmine 標準の Rails i18n 機能（`config/locales/en.yml`、`config/locales/ja.yml`）を使用
- View・Controller・モデルのバリデーションメッセージすべて `l(:key)` / `t(:key)` 経由で出力
- ハードコードされた日本語文字列は一切禁止
- 対応ロケール：`en`（英語）、`ja`（日本語）

---

## 8. 技術スタック・動作環境

- Redmine 6.0.x（Docker Compose 環境での動作を優先確認）
- Ruby on Rails プラグイン標準構成
- フロントエンド：Redmine 標準 jQuery + 軽量 JS によるカレンダー実装
- 外部依存：Nager.Date API（祝日オンライン取得時のみ）
- プラグイン名：`redmine_non_working_days`
- Puma：`workers 4`、`threads 1,1`、`preload_app!`
- キャッシュバックエンド：`:file_store`

---

## 付録：DB テーブル設計（案）

### `non_working_day_entries`（holiday / custom_fixed 用）

| カラム | 型 | 説明 |
|---|---|---|
| `id` | integer | PK |
| `date` | date | 非稼働日の日付 |
| `title` | string | 休日タイトル |
| `kind` | string | `holiday` / `custom_fixed` |
| `country_code` | string | 国コード（holiday のみ。それ以外は NULL） |
| `created_at` | datetime | |
| `updated_at` | datetime | |

### `non_working_day_rules`（custom_recurring 用）

| カラム | 型 | 説明 |
|---|---|---|
| `id` | integer | PK |
| `title` | string | 休日タイトル |
| `rule_type` | string | `monthly_day` / `monthly_last` / `monthly_nth_weekday` / `biweekly` |
| `rule_params` | json | ルール固有パラメータ（下記参照） |
| `start_date` | date | 有効開始日（NULL = 無制限）|
| `end_date` | date | 有効終了日（NULL = 無期限）|
| `created_at` | datetime | |
| `updated_at` | datetime | |

**`rule_params` の例：**

```json
// monthly_day：毎月 n 日
{ "days": [10, 15, 20] }

// monthly_last：毎月末日
{}

// monthly_nth_weekday：毎月第 m × 曜日
{ "nth": 2, "weekday": 1 }
// nth: 1〜5、weekday: 0=日〜6=土

// biweekly：隔週 × 曜日
{ "weekday": 5 }
// weekday: 0=日〜6=土
// 起点は start_date の範囲内で最初に指定曜日が現れる日
```
